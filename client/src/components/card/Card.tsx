import React, { useState } from "react";

function Card() {
  const [data, setData] = useState({
    name: String,
    review: Array,
    description: String,
  });
  return (
    <div>
      <h2></h2>
    </div>
  );
}

export default Card;
